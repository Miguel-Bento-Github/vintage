# Firebase Configuration
# Get these values from your Firebase Console: https://console.firebase.google.com
# Go to Project Settings > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_firebase_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# Firebase Emulators (Development Only)
# Set to 'true' to use local Firebase emulators
# Emulator ports: 3476 (Firestore), 3477 (Auth), 3478 (Storage), 3479 (UI)
# Run `firebase emulators:start` before starting the dev server
NEXT_PUBLIC_USE_FIREBASE_EMULATORS=false

# Stripe Configuration
# Get these from: https://dashboard.stripe.com/apikeys
# Use test keys (pk_test_* and sk_test_*) for development
# Use live keys (pk_live_* and sk_live_*) for production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe Webhook Secret
# For production: Get this from https://dashboard.stripe.com/webhooks
# After creating a webhook endpoint pointing to: https://yourdomain.com/api/webhook
# Events to listen for: payment_intent.succeeded, payment_intent.payment_failed, payment_intent.canceled
#
# For local development with Stripe CLI:
# 1. Install Stripe CLI: https://stripe.com/docs/stripe-cli
# 2. Run: stripe listen --forward-to localhost:5577/api/webhook
# 3. Copy the webhook signing secret (starts with whsec_) from the CLI output
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# SendCloud Configuration (Optional - for real-time EU shipping rates)
# SendCloud provides multi-carrier shipping for Europe (PostNL, DHL, UPS, DPD)
# Get these from: https://panel.sendcloud.sc/settings/integrations
# If not configured, the app will use static fallback shipping rates
#
# Setup:
# 1. Sign up at https://www.sendcloud.com
# 2. Go to Settings > Integrations > API
# 3. Create API credentials (Public Key & Secret Key)
# 4. Configure your warehouse address in /lib/sendcloud.ts
# 5. Add shipping methods in SendCloud panel
SENDCLOUD_PUBLIC_KEY=your_sendcloud_public_key_here
SENDCLOUD_SECRET_KEY=your_sendcloud_secret_key_here

# Warehouse Address for Shipping Calculations
# Used by SendCloud API to calculate shipping rates from your location
WAREHOUSE_NAME=Your Store Name
WAREHOUSE_ADDRESS=Your Street Address
WAREHOUSE_CITY=Amsterdam
WAREHOUSE_POSTAL_CODE=1012AB
WAREHOUSE_COUNTRY=NL
